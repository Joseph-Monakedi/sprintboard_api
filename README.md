# SprintBoard Task Tracker API

SprintBoard is a C# Minimal API task tracker for creating, assigning, and moving team tasks through `Backlog`, `InProgress`, `InReview`, and `Done`. It demonstrates nullable reference types, events, delegates, SOLID interfaces, and an in-memory repository.

Run it locally with:

```bash
dotnet run
```
